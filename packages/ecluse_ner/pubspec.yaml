name: ecluse_ner
description: >-
  Détecteur statistique (NER) pour Écluse — stub pour l'instant, sans
  dépendance native. Le modèle (flutter_onnxruntime) n'entre que dans la
  phase NER dédiée.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  ecluse_core:
    path: ../ecluse_core

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
