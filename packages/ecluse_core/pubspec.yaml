name: ecluse_core
description: >-
  Détection d'entités personnelles françaises et européennes (PII) pour
  Écluse — le sas RGPD entre vos prompts et les LLM. Zéro dépendance réseau,
  100 % on-device.
version: 0.1.0
repository: https://github.com/ecluse-dev/ecluse
publish_to: none # retiré lors de la première publication pub.dev

environment:
  sdk: ">=3.3.0 <4.0.0"

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
